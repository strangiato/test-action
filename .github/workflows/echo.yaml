name: echo
on:
  workflow_call:
    inputs:
      echo-value:
        required: true
        type: string
jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: echo
        run: |
          echo ${{ inputs.echo-value }}
