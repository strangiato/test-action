name: my-test-action
on:
  push: {}

jobs:
  test-action-1:
    env:
      ECHO: "hello"
    uses: ./.github/workflows/echo.yaml
  test-action-2:
    env:
      ECHO: "world"
    uses: ./.github/workflows/echo.yaml
